# Core dependencies - Windows Python 3.14+ compatible versions
httpx>=0.27.0
beautifulsoup4>=4.12.3
sqlalchemy>=2.0.36

# Database drivers (choose one)
# For PostgreSQL:
# psycopg2-binary==2.9.9

# For MySQL (not needed on Windows - using SQLite):
# mysql-connector-python>=8.3.0

# Optional but recommended
python-dotenv>=1.0.1

# Playwright for Cloudflare bypass (CasinoMeister, OwnedCore)
# Updated versions with Python 3.14 prebuilt wheels
playwright>=1.48.0
playwright-stealth>=2.0.1
